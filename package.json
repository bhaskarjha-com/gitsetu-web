{
  "name": "gitsetu-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync-docs": "./scripts/sync_docs.sh",
    "dev": "npm run sync-docs && astro dev",
    "build": "npm run sync-docs && astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^6.3.1"
  }
}
